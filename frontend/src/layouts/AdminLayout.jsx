import {NavLink,Outlet} from 'react-router-dom';import {useState} from 'react';import {useAuth} from '../context/AuthContext';
const links=[['/admin','Tổng quan'],['/admin/products','Món ăn'],['/admin/categories','Danh mục'],['/admin/orders','Đơn hàng'],['/admin/users','Người dùng'],['/admin/payments','Thanh toán']];
export default function AdminLayout(){const[open,setOpen]=useState(false);const{user,logout}=useAuth();return <div className="admin-shell"><aside className={`admin-sidebar ${open?'open':''}`}><div className="admin-brand">TALO<span>FOOD</span><small>ADMIN</small></div><nav>{links.map(([to,label],i)=><NavLink key={to} end={i===0} to={to} onClick={()=>setOpen(false)}>{label}</NavLink>)}</nav><NavLink to="/">Về trang khách hàng</NavLink><button onClick={logout}>Đăng xuất</button></aside><section className="admin-main"><header className="admin-topbar"><button className="admin-menu" onClick={()=>setOpen(!open)}>☰</button><div><strong>{user?.full_name}</strong><span>Quản trị viên</span></div></header><div className="admin-content"><Outlet/></div></section></div>}
