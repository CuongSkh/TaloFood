import { useCallback,useEffect,useState } from 'react';import { Link } from 'react-router-dom';import { ordersApi } from '../api/ordersApi';import { formatPrice } from '../components/ProductCard';
const OrderHistoryPage=()=>{const[data,setData]=useState(null);const[error,setError]=useState('');const load=useCallback(async()=>{try{setData(await ordersApi.mine());setError('')}catch(e){setError(e.userMessage||'Không thể tải lịch sử đơn hàng.')}},[]);useEffect(()=>{load()},[load]);if(error)return <main className="commerce-page container"><div className="empty-commerce"><h2>{error}</h2><button onClick={load}>Thử lại</button></div></main>;if(!data)return <main className="commerce-page container"><div className="commerce-state">Đang tải lịch sử đơn hàng...</div></main>;return <main className="commerce-page container"><div className="commerce-title"><p className="eyebrow">Tài khoản</p><h1>ĐƠN HÀNG CỦA TÔI</h1></div>{!data.items.length?<div className="empty-commerce"><h2>Bạn chưa có đơn hàng nào</h2><Link className="button button--primary" to="/products">Xem thực đơn</Link></div>:<div className="order-list">{data.items.map(o=><article className="order-card" key={o.id}><div><span className="status-pill">{o.order_status}</span><h2>{o.order_code}</h2><p>{new Date(o.created_at).toLocaleString('vi-VN')}</p></div><div><span>{o.total_quantity} món</span><strong>{formatPrice(o.total_amount)}</strong><span>COD · {o.payment_status}</span></div><Link to={`/orders/${o.id}`}>Xem chi tiết →</Link></article>)}</div>}</main>};export default OrderHistoryPage;
