import{useEffect,useState}from'react';import{adminApi}from'../../api/adminApi';import{formatPrice}from'../../components/ProductCard';import StatCard from'../../components/admin/StatCard';import StatusBadge from'../../components/admin/StatusBadge';
export default function AdminDashboardPage(){const[d,setD]=useState(null);const[e,setE]=useState('');const load=async()=>{try{const[o,m,r,t,s]=await Promise.all([adminApi.overview(),adminApi.monthlyRevenue(),adminApi.recentOrders(),adminApi.topProducts(),adminApi.orderStatus()]);setD({o,m,r,t,s})}catch(x){setE(x.response?.data?.detail||'Không thể tải dashboard')}};useEffect(()=>{load()},[]);if(e)return <div className="admin-error">{e}<button onClick={load}>Thử lại</button></div>;if(!d)return <div className="admin-loading">Đang tải dashboard...</div>;const max=Math.max(...d.m.map(x=>x.revenue),1);return <><div className="admin-heading"><div><p className="eyebrow">Tổng quan</p><h1>Dashboard TaloFood</h1></div></div><div className="stats-grid"><StatCard label="Tổng sản phẩm" value={d.o.total_products}/><StatCard label="Tổng đơn hàng" value={d.o.total_orders}/><StatCard label="Tổng người dùng" value={d.o.total_users}/><StatCard label="Tổng doanh thu" value={formatPrice(d.o.total_revenue)}/></div><div className="admin-grid-2"><section className="admin-panel"><h2>Doanh thu theo tháng</h2><div className="revenue-chart">{d.m.length?d.m.map(x=><div className="bar-wrap" key={x.month}><div className="bar" style={{height:`${Math.max(8,x.revenue/max*180)}px`}} title={formatPrice(x.revenue)}/><span>{x.month}</span></div>):<p>Chưa có doanh thu đã thanh toán.</p>}</div></section><section className="admin-panel"><h2>Top món bán chạy</h2>{d.t.map((x,i)=><div className="rank-row" key={x.product_name}><b>#{i+1}</b><span>{x.product_name}</span><strong>{x.quantity} món</strong></div>)}</section></div><section className="admin-panel"><h2>Đơn hàng gần đây</h2><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Mã đơn</th><th>Người nhận</th><th>Tổng tiền</th><th>Thanh toán</th><th>Trạng thái</th></tr></thead><tbody>{d.r.map(o=><tr key={o.id}><td>{o.order_code}</td><td>{o.receiver_name}</td><td>{formatPrice(o.total_amount)}</td><td><StatusBadge value={o.payment_status}/></td><td><StatusBadge value={o.order_status}/></td></tr>)}</tbody></table></div></section></>}
