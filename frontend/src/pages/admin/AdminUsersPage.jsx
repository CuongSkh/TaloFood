import{useEffect,useState}from'react';import{adminApi}from'../../api/adminApi';import StatusBadge from'../../components/admin/StatusBadge';export default function AdminUsersPage(){const[data,setData]=useState({items:[]}),[search,setSearch]=useState(''),[error,setError]=useState('');const load=async()=>{try{setData(await adminApi.users({search}))}catch(e){setError(e.response?.data?.detail||'Không thể tải người dùng')}};useEffect(()=>{load()},[]);return <><div className="admin-heading"><div><p className="eyebrow">Quản lý</p><h1>Người dùng</h1></div></div><div className="admin-toolbar"><input placeholder="Tên hoặc email" value={search} onChange={e=>setSearch(e.target.value)}/><button onClick={load}>Tìm</button></div>{error&&<div className="admin-error">{error}</div>}<section className="admin-panel"><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Họ tên</th><th>Email</th><th>Role</th><th>Trạng thái</th><th></th></tr></thead><tbody>{data.items.map(u=><tr key={u.id}><td><b>{u.full_name}</b></td><td>{u.email}</td><td><StatusBadge value={u.role}/></td><td><StatusBadge value={u.is_active?'ACTIVE':'LOCKED'}/></td><td><button onClick={async()=>{try{await adminApi.updateUserStatus(u.id,!u.is_active);load()}catch(e){setError(e.response?.data?.detail)}}}>{u.is_active?'Khóa':'Mở'}</button><button onClick={async()=>{try{await adminApi.updateUserRole(u.id,u.role==='ADMIN'?'CUSTOMER':'ADMIN');load()}catch(e){setError(e.response?.data?.detail)}}}>Đổi role</button></td></tr>)}</tbody></table></div></section></>}
