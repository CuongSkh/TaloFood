import{useCallback,useEffect,useState}from'react';import{Link,useSearchParams}from'react-router-dom';import{paymentsApi}from'../api/paymentsApi';import{formatPrice}from'../components/ProductCard';
export default function StripeSuccessPage(){const[p]=useSearchParams();const sid=p.get('session_id');const[data,setData]=useState(null);const[error,setError]=useState('');const[loading,setLoading]=useState(true);const verify=useCallback(async()=>{setLoading(true);setError('');try{if(!sid)throw new Error('Thiếu Stripe session ID');setData(await paymentsApi.verifyStripe(sid))}catch(e){setError(e.response?.data?.detail||e.message||'Không thể xác minh thanh toán')}finally{setLoading(false)}},[sid]);useEffect(()=>{verify()},[verify]);return <main className="commerce-page container"><section className="payment-result">{loading?<h1>Đang xác minh thanh toán...</h1>:error?<><h1>Chưa thể xác minh thanh toán</h1><p>{error}</p><button onClick={verify}>Thử lại</button><Link to="/orders">Xem đơn hàng</Link></>:<><p className="eyebrow">STRIPE VERIFIED</p><h1>THANH TOÁN THÀNH CÔNG</h1><p>Mã đơn: <strong>{data.order_code}</strong></p><p>Tổng tiền: <strong>{formatPrice(data.amount)}</strong></p><p>Trạng thái: <strong>{data.order_payment_status}</strong></p><Link to={`/orders/${data.order_id}`}>Xem chi tiết đơn</Link><Link to="/">Về trang chủ</Link></>}</section></main>}
